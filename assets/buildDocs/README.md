# Environment
* Node.js v12.6.3
- npm v6.14.4

# Install
```bash
# install the gitbook CLI dependency
npm i -g gitbook-cli
# install the gitbook plugin 
gitbook install
# build
gitbook build
# start
gitbook serve
```